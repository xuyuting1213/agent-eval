---
description: Nuxt server API 约定与错误处理规范
globs: "server/**/*.ts"
alwaysApply: false
---

# Server API Rules

- 接口文件放在 `server/api/`，命名清晰表达资源与动作。
- 每个 handler 做输入校验；对无效参数返回明确错误信息。
- 统一错误处理：捕获预期异常并返回可读消息，避免泄露内部栈信息。
- 与数据库或外部服务交互时，记录关键上下文并保留最小必要日志。
- 响应结构保持一致，便于前端稳定解析。
- 涉及鉴权的接口必须显式校验身份与权限，不依赖前端约束。
